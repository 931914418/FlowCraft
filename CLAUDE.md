# FlowCraft 项目规则

## 完成闸门 — 报告完成前必须走完

以下步骤严格按顺序执行，上一步不通过就不能到下一步。
全部通过后才能说"完成"，否则如实报告卡在哪一步。

### Step 1: 定义成功标准（改代码之前）

用一句话描述用户视角的预期结果，不接受纯技术指标。

- BAD: "API 返回 200"
- BAD: "编译无报错"
- GOOD: "用户上传 UI 设计稿后，得到 3+ 个文件，路径包含 pages/、api/、types/"
- **GOOD（交互功能）**: "用户点击节点 → 右侧显示配置 → 修改 Model → 保存 → 刷新页面 → 重新打开节点 → Model 配置保持不变"

如果无法描述清楚成功标准 → 说明对任务理解不够，先跟用户确认。

### Step 2: 改代码

批量完成所有关联改动，不要边改边验。遇到以下场景单独处理：

- **配置改动**（.env / 依赖 / config）→ 改完立即确认配置能被读取，再继续其他改动
- **不熟悉的导入** → 用 `node -e "console.log(Object.keys(require('pkg')))"` 验证导出存在，再写 import
- **数据结构改动** → 必须验证前后端契约一致，检查加载、编辑、保存、重新加载的完整流程

### Step 3: 验证（改代码之后，报告之前）

1. **重启服务**（配置改动必须重启）
2. **清除浏览器缓存**（Ctrl+Shift+R）
3. **端到端验证**，覆盖成功标准中描述的用户路径
4. **交互验证**（如涉及 UI）：
   - 操作 → 预期反馈是否出现
   - 修改 → 保存 → 刷新 → 回显是否正确
   - 切换 → 再切换 → 配置是否保持
5. 收集证据（终端日志 / 截图 / 查询结果）

### Step 4: 对照标准判读

- 验证结果 **匹配** 成功标准 → 报告完成，附上证据
- 验证结果 **不匹配** → 如实说差距在哪，不修改标准、不换简单输入凑结论
- 遇到报错需要诊断 → 先用最简方式复现（curl / node -e），再给解释，不凭直觉归因

### Step 5: 数据契约验证（数据相关改动必做）

涉及数据结构的改动，必须验证：

- [ ] 后端 Schema → API 响应 → 前端类型 → 运行时数据，四者一致
- [ ] 保存 → 数据库 → 重新加载 → 回显，完整链路一致
- [ ] 切换操作 → 数据保持，无意外重置

### 黄金路径测试清单（UI 功能必做）

任何涉及用户交互的功能，必须验证：

- [ ] 创建新对象
- [ ] 编辑属性
- [ ] 保存更改
- [ ] 刷新页面
- [ ] 验证回显正确
- [ ] 修改配置
- [ ] 切换到其他对象再切换回来
- [ ] 验证配置保持不变

---

## 第一性原则

### 数据契约原则

数据结构必须在所有层级保持一致：

```
数据库 Schema ← API 定义 ← 前端类型 ← 运行时数据
     ↑_______________|
      契约必须一致，任何层级破坏都会导致数据丢失
```

### 验证完整性原则

- 单元测试 ≠ 集成测试 ≠ E2E 测试
- API 成功 ≠ UI 可用
- 保存成功 ≠ 回显正确
- 代码修复 ≠ 用户可见修复

### 错误处理原则

- 所有用户可见的错误必须有友好提示
- 控制台错误 ≠ 用户错误
- 静默失败 ≠ 正常运行
